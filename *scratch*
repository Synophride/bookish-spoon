;; This buffer is for notes you don't want to save, and for Lisp evaluation.
;; If you want to create a file, visit that file with C-x C-f,
;; then enter the text in that file's own buffer.

Set.make(String)

let general_evt = SS.create_void      (paire id -> g_t) (?ref) 
let instancied_evt = SS.create_void (paire id -> t) (pas forcément ref)


à chaque déclaration de fonction/var, obtention d'un type instancié
  -> généralisation + SS.put general_evt (id g_t)
à chaque application de fonction
  - Recherche de la primitive dans les tables
  tentative d'unification + modifier la/les réferences si nécessaire

constante -> trivial
primitive -> instance triviale du type (genre son type généralisé)

Fonction genre (fun x -> x)
	 -> typer la fonction en instanciant x (créant une nouvelle variable de type)

+ HM (identificateur -> type) 

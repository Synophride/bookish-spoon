
(* Note : ?substitution des valeurs ou des types ? *)
(** Substitution de x par valeur dans l'expression expr **)
let subst expr truc_remplace valeur_remplacante valeur = 
  let rec subst_pexpr pexpr truc_remplace valeur_remplacante =
    subst_desc 
  and subst_desc pexpr_d remplace remplacant valeur =
    match pexpr
    
;;
